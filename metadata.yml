name: EssentialCC
version: V1.0.1
status: STABLE
message: |
  # EssentialCC V1.0.1
  - Added more slash commands:
    - `/warp`
    - `/delwarp`
    - `/setwarp`
    - `/warplist`
    - `/kickall`
    - `/mute`
    - `/realname`
    - `/unmute`
    - `/suicide`
  - Fixed an issue with movement detector during teleportation